# 🏗️ Architecture Overview

## System Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                        USER BROWSER                          │
│                    http://yt.quizb.my.id                     │
└────────────────────────┬────────────────────────────────────┘
                         │
                         │ HTTP Request
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                     FLASK APPLICATION                        │
│                        (app.py)                              │
│  ┌────────────────────────────────────────────────────┐    │
│  │  Routes:                                            │    │
│  │  • GET  /             → index.html                 │    │
│  │  • POST /api/info     → Video Info                 │    │
│  │  • POST /api/download → Download Video             │    │
│  │  • GET  /api/check-ffmpeg → Check ffmpeg          │    │
│  └────────────────────────────────────────────────────┘    │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                      YT-DLP LIBRARY                          │
│                   (yt_dlp module)                            │
│  ┌────────────────────────────────────────────────────┐    │
│  │  Enhanced Options:                                  │    │
│  │  • Multiple Player Clients (Android, Web)          │    │
│  │  • Enhanced HTTP Headers (Firefox UA)              │    │
│  │  • Retry Mechanism (10x retries)                   │    │
│  │  • Fragment Retries (10x)                          │    │
│  │  • Exponential Backoff                             │    │
│  └────────────────────────────────────────────────────┘    │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                         YOUTUBE                              │
│                   (youtube.com API)                          │
│  ┌────────────────────────────────────────────────────┐    │
│  │  • Video Metadata                                   │    │
│  │  • Video Streams (multiple qualities)              │    │
│  │  • Audio Streams                                    │    │
│  └────────────────────────────────────────────────────┘    │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                          FFMPEG                              │
│                   (Video Processing)                         │
│  ┌────────────────────────────────────────────────────┐    │
│  │  • Merge Video + Audio                              │    │
│  │  • Convert to MP4                                   │    │
│  │  • Re-encode if needed                              │    │
│  └────────────────────────────────────────────────────┘    │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                    TEMPORARY STORAGE                         │
│                        (tmp/)                                │
│  ┌────────────────────────────────────────────────────┐    │
│  │  • Downloaded video fragments                       │    │
│  │  • Merged MP4 file                                  │    │
│  │  • Auto cleanup after download                      │    │
│  └────────────────────────────────────────────────────┘    │
└────────────────────────┬────────────────────────────────────┘
                         │
                         │ Send File
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                      USER DOWNLOADS                          │
│                      video.mp4                               │
└─────────────────────────────────────────────────────────────┘
```

## Request Flow

### 1. Video Info Request
```
User → Flask → yt-dlp → YouTube API
                ↓
         Extract Metadata
                ↓
         Return JSON
```

### 2. Video Download Request
```
User → Flask → yt-dlp → YouTube API
                ↓
         Download Video Stream
                ↓
         Download Audio Stream
                ↓
         ffmpeg Merge
                ↓
         Temp File (MP4)
                ↓
         Send to User
                ↓
         Auto Cleanup
```

## Anti-403 Flow

```
┌─────────────────────────┐
│   User Request          │
└───────────┬─────────────┘
            │
            ▼
┌─────────────────────────┐
│  get_ydl_opts()         │
│  • Enhanced Headers     │
│  • Multiple Clients     │
│  • Retry Settings       │
└───────────┬─────────────┘
            │
            ▼
┌─────────────────────────┐
│   Attempt 1             │
│   Player: Android       │
└───────────┬─────────────┘
            │
            ├──[SUCCESS]──→ Download
            │
            └──[FAIL 403]──→
                            │
                            ▼
            ┌─────────────────────────┐
            │   Wait 2^0 = 1 sec      │
            └───────────┬─────────────┘
                        │
                        ▼
            ┌─────────────────────────┐
            │   Attempt 2             │
            │   Player: Web           │
            └───────────┬─────────────┘
                        │
                        ├──[SUCCESS]──→ Download
                        │
                        └──[FAIL 403]──→
                                        │
                                        ▼
                        ┌─────────────────────────┐
                        │   Wait 2^1 = 2 sec      │
                        └───────────┬─────────────┘
                                    │
                                    ▼
                        ┌─────────────────────────┐
                        │   Attempt 3             │
                        │   Player: Android       │
                        └───────────┬─────────────┘
                                    │
                                    ├──[SUCCESS]──→ Download
                                    │
                                    └──[FAIL]─────→ Error 403
```

## File Structure

```
yt.quizb.my.id/
│
├── app.py                    # Main Flask application
├── passenger_wsgi.py         # WSGI entry point for cPanel
├── requirements.txt          # Python dependencies
├── update_ytdlp.py          # Update script
│
├── templates/
│   └── index.html           # Web UI
│
├── tmp/
│   ├── restart.txt          # Touch to restart on cPanel
│   └── [temp downloads]     # Temporary video files
│
├── .venv/                   # Virtual environment (local)
│
├── __pycache__/             # Python cache
│
└── Documentation:
    ├── README.md            # Full documentation
    ├── QUICKSTART.md        # Setup guide
    ├── CHANGELOG.md         # Version history
    ├── COMMANDS.md          # Command reference
    ├── CHANGES_SUMMARY.md   # Summary of changes
    └── ARCHITECTURE.md      # This file
```

## Data Flow Diagram

```
┌──────────┐
│  Client  │
└────┬─────┘
     │
     │ POST /api/download
     │ {"url": "..."}
     │
     ▼
┌────────────────┐
│  Flask Router  │
└────┬───────────┘
     │
     │ Validate URL
     │ Check ffmpeg
     │
     ▼
┌────────────────────┐
│  Create Temp Dir   │
│  tmpdir = /tmp/xxx │
└────┬───────────────┘
     │
     ▼
┌────────────────────┐
│  Initialize yt-dlp │
│  with options      │
└────┬───────────────┘
     │
     ▼
┌────────────────────┐
│  Retry Loop (3x)   │
└────┬───────────────┘
     │
     ├─→ Attempt 1
     │   ├─→ [Success] → Continue
     │   └─→ [Fail] → Wait 1s
     │
     ├─→ Attempt 2
     │   ├─→ [Success] → Continue
     │   └─→ [Fail] → Wait 2s
     │
     └─→ Attempt 3
         ├─→ [Success] → Continue
         └─→ [Fail] → Return Error
                        │
                        ▼
            ┌────────────────────┐
            │  Download Stream   │
            │  (Video + Audio)   │
            └────┬───────────────┘
                 │
                 ▼
            ┌────────────────────┐
            │  ffmpeg Merge      │
            │  → output.mp4      │
            └────┬───────────────┘
                 │
                 ▼
            ┌────────────────────┐
            │  Send File to User │
            └────┬───────────────┘
                 │
                 ▼
            ┌────────────────────┐
            │  Cleanup Temp Dir  │
            └────────────────────┘
```

## Component Interaction

```
┌─────────────────────────────────────────────────────────┐
│                    FRONTEND (index.html)                 │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐    │
│  │   Input     │  │   Button    │  │   Spinner   │    │
│  │   URL       │  │   Download  │  │   Loading   │    │
│  └─────────────┘  └─────────────┘  └─────────────┘    │
│                                                          │
│  JavaScript:                                             │
│  • Validate URL                                          │
│  • Send POST request                                     │
│  • Handle response                                       │
│  • Trigger download                                      │
└─────────────────────────┬───────────────────────────────┘
                          │
                          │ fetch()
                          ▼
┌─────────────────────────────────────────────────────────┐
│                    BACKEND (app.py)                      │
│                                                          │
│  ┌────────────────────────────────────────────────┐    │
│  │  api_download()                                 │    │
│  │  • Validate URL                                 │    │
│  │  • Check ffmpeg                                 │    │
│  │  • Create temp directory                        │    │
│  │  • Configure yt-dlp                             │    │
│  │  • Download with retry                          │    │
│  │  • Send file                                    │    │
│  │  • Cleanup                                      │    │
│  └────────────────────────────────────────────────┘    │
│                                                          │
│  ┌────────────────────────────────────────────────┐    │
│  │  get_ydl_opts()                                 │    │
│  │  • Build configuration                          │    │
│  │  • Set headers                                  │    │
│  │  • Set retry counts                             │    │
│  │  • Set format selection                         │    │
│  └────────────────────────────────────────────────┘    │
└─────────────────────────┬───────────────────────────────┘
                          │
                          │ extract_info()
                          ▼
┌─────────────────────────────────────────────────────────┐
│                  YT-DLP (yt_dlp library)                 │
│                                                          │
│  ┌────────────────────────────────────────────────┐    │
│  │  YoutubeDL Class                                │    │
│  │  • Parse URL                                    │    │
│  │  • Select extractor                             │    │
│  │  • Make HTTP requests                           │    │
│  │  • Handle retries                               │    │
│  │  • Download fragments                           │    │
│  │  • Merge streams                                │    │
│  └────────────────────────────────────────────────┘    │
└─────────────────────────┬───────────────────────────────┘
                          │
                          │ HTTP Requests
                          ▼
┌─────────────────────────────────────────────────────────┐
│                    YOUTUBE SERVERS                       │
│  • API endpoints                                         │
│  • Video streams                                         │
│  • Audio streams                                         │
│  • Metadata                                              │
└─────────────────────────────────────────────────────────┘
```

## Error Handling Flow

```
┌────────────┐
│   Error    │
└─────┬──────┘
      │
      ├──[403 Forbidden]─→ Retry with different client
      │                    Exponential backoff
      │                    Show specific error message
      │
      ├──[Network Error]─→ Retry up to 10 times
      │                    Fragment retry
      │
      ├──[No ffmpeg]────→ Return error
      │                   Suggest local yt-dlp
      │
      ├──[Invalid URL]──→ Validate and return error
      │
      └──[Other Error]──→ Log error
                         Cleanup temp files
                         Return generic error
```

## Deployment Architecture

### Development
```
Localhost:5000
    ↓
Flask Dev Server
    ↓
app.py
    ↓
Local .venv
```

### Production (cPanel)
```
yt.quizb.my.id
    ↓
Apache/Nginx
    ↓
Passenger WSGI
    ↓
passenger_wsgi.py
    ↓
app.py
    ↓
Server Python Environment
```

## Security Layers

```
1. Input Validation
   └→ URL validation
   └→ Parameter sanitization

2. Rate Limiting
   └→ Socket timeout (30s)
   └→ Retry delays

3. File Management
   └→ Temp directory isolation
   └→ Auto cleanup
   └→ Filename sanitization

4. Error Handling
   └→ No sensitive data in errors
   └→ Generic error messages
   └→ Logging

5. CORS
   └→ flask-cors configuration
   └→ Allowed origins
```

## Performance Optimization

```
1. Async Operations
   └→ Background downloads
   └→ Non-blocking cleanup

2. Caching
   └→ Python bytecode
   └→ Static files

3. Resource Management
   └→ Temp file cleanup
   └→ Memory efficient streaming
   └→ Fragment handling

4. Retry Strategy
   └→ Exponential backoff
   └→ Smart retry logic
   └→ Avoid thundering herd
```

---

## 🔧 Configuration Options

### yt-dlp Options
```python
{
    'format': 'best[ext=mp4]',           # Format selection
    'quiet': True,                        # Suppress output
    'no_warnings': True,                  # No warnings
    'noplaylist': True,                   # Single video only
    'socket_timeout': 30,                 # 30s timeout
    'retries': 10,                        # 10 retries
    'fragment_retries': 10,               # 10 fragment retries
    'extractor_retries': 3,               # 3 extractor retries
    'http_headers': {...},                # Custom headers
    'extractor_args': {...}               # Extractor arguments
}
```

### Flask Configuration
```python
app.config['DEBUG'] = True               # Debug mode
app.config['MAX_CONTENT_LENGTH'] = None  # No size limit
CORS(app)                                # Enable CORS
```

## Monitoring Points

1. **Application Health**
   - Server uptime
   - Response times
   - Error rates

2. **Resource Usage**
   - Disk space (tmp/)
   - Memory usage
   - CPU usage

3. **External Dependencies**
   - yt-dlp version
   - ffmpeg availability
   - YouTube API status

4. **Error Tracking**
   - 403 error frequency
   - Download failures
   - Network errors

---

**Last Updated**: 2024-12-23  
**Version**: 2.0.0
